# Test Plan: Login.md

> **Generated by QA Agent** on 2025-11-26T16:01:00.913Z

## Requirements Coverage

### REQ-001: User must be able to log in with email and password
- **Category**: functional
- **Test Cases**: 21

## Test Cases

### TC-001: Valid login with correct credentials

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with valid email and password

**Steps**:
1. Navigate to login page
2. Enter valid email
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-002: Valid login with incorrect password

**Type**: negative | **Requirement**: REQ-001

**Description**: Verify user cannot log in with invalid password

**Steps**:
1. Navigate to login page
2. Enter valid email
3. Enter incorrect password
4. Click login

**Expected Result**: User is redirected to login page with error message

---

### TC-003: Valid login with empty password field

**Type**: negative | **Requirement**: REQ-001

**Description**: Verify user cannot log in with an empty password field

**Steps**:
1. Navigate to login page
2. Enter valid email
3. Leave password field blank
4. Click login

**Expected Result**: User is redirected to login page with error message

---

### TC-004: Valid login with empty email field

**Type**: negative | **Requirement**: REQ-001

**Description**: Verify user cannot log in with an empty email field

**Steps**:
1. Navigate to login page
2. Leave email field blank
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to login page with error message

---

### TC-005: Valid login with special characters in password

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with a password containing special characters

**Steps**:
1. Navigate to login page
2. Enter valid email
3. Enter password containing special characters
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-006: Valid login with invalid email format

**Type**: negative | **Requirement**: REQ-001

**Description**: Verify user cannot log in with an invalid email format

**Steps**:
1. Navigate to login page
2. Enter invalid email format
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to login page with error message

---

### TC-007: Valid login with long password

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with a password exceeding the maximum allowed length

**Steps**:
1. Navigate to login page
2. Enter valid email
3. Enter password exceeding the maximum allowed length
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-008: Valid login with short password

**Type**: negative | **Requirement**: REQ-001

**Description**: Verify user cannot log in with a password shorter than the minimum allowed length

**Steps**:
1. Navigate to login page
2. Enter valid email
3. Enter password shorter than the minimum allowed length
4. Click login

**Expected Result**: User is redirected to login page with error message

---

### TC-009: Valid login with special characters in email

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with an email containing special characters

**Steps**:
1. Navigate to login page
2. Enter valid email containing special characters
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-010: Valid login with invalid email format and special characters in password

**Type**: negative | **Requirement**: REQ-001

**Description**: Verify user cannot log in with an invalid email format and a password containing special characters

**Steps**:
1. Navigate to login page
2. Enter invalid email format and special characters in password
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to login page with error message

---

### TC-011: Valid login with long email

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with an email exceeding the maximum allowed length

**Steps**:
1. Navigate to login page
2. Enter valid email exceeding the maximum allowed length
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-012: Valid login with short email

**Type**: negative | **Requirement**: REQ-001

**Description**: Verify user cannot log in with an email shorter than the minimum allowed length

**Steps**:
1. Navigate to login page
2. Enter valid email shorter than the minimum allowed length
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to login page with error message

---

### TC-013: Valid login with special characters in email and password

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with an email containing special characters and a password containing special characters

**Steps**:
1. Navigate to login page
2. Enter valid email containing special characters and a password containing special characters
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-014: Valid login with long email and short password

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with an email exceeding the maximum allowed length and a password shorter than the minimum allowed length

**Steps**:
1. Navigate to login page
2. Enter valid email exceeding the maximum allowed length and a password shorter than the minimum allowed length
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-015: Valid login with short email and long password

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with an email shorter than the minimum allowed length and a password exceeding the maximum allowed length

**Steps**:
1. Navigate to login page
2. Enter valid email shorter than the minimum allowed length and a password exceeding the maximum allowed length
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-016: Valid login with special characters in email and long password

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with an email containing special characters and a password exceeding the maximum allowed length

**Steps**:
1. Navigate to login page
2. Enter valid email containing special characters and a password exceeding the maximum allowed length
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-017: Valid login with long email and special characters in password

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with an email exceeding the maximum allowed length and a password containing special characters

**Steps**:
1. Navigate to login page
2. Enter valid email exceeding the maximum allowed length and a password containing special characters
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-018: Valid login with short email and long password and special characters in password

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with an email shorter than the minimum allowed length, a password exceeding the maximum allowed length, and a password containing special characters

**Steps**:
1. Navigate to login page
2. Enter valid email shorter than the minimum allowed length, a password exceeding the maximum allowed length, and a password containing special characters
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-019: Valid login with long email and short password and special characters in password

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with an email exceeding the maximum allowed length, a password shorter than the minimum allowed length, and a password containing special characters

**Steps**:
1. Navigate to login page
2. Enter valid email exceeding the maximum allowed length, a password shorter than the minimum allowed length, and a password containing special characters
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-020: Valid login with short email and long password and special characters in password

**Type**: positive | **Requirement**: REQ-001

**Description**: Verify user can log in with an email shorter than the minimum allowed length, a password exceeding the maximum allowed length, and a password containing special characters

**Steps**:
1. Navigate to login page
2. Enter valid email shorter than the minimum allowed length, a password exceeding the maximum allowed length, and a password containing special characters
3. Enter valid password
4. Click login

**Expected Result**: User is redirected to dashboard

---

### TC-NEW-001: Missing edge case title

**Type**: edge | **Requirement**: REQ-001

**Description**: Description of what's missing

**Steps**:
1. Step 1
2. Step 2

**Expected Result**: Expected result

---



<!-- Debug: Requirements: 1, Tests: 21 -->